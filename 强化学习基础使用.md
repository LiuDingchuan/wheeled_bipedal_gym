### 强化学习基础使用

> 主要包含IssacGym的基础使用方法，和利用ros_control在gazebo中进行sim2sim的测试的流程。意在快速使用并能够更换urdf模型利用强化学习训练自己的机器人。



#### RL基础知识

> https://b23.tv/rUxok7O

​	简单来说`强化学习是一种自探索的控制方法，通过URDF内的定义的机器人，人为设计的环境以及奖励，让机器人在与环境的交互中学习中能利用自身行为获得人为给定的高奖励，以此达到控制机器人的目的`，不同于基于模型的控制理论，强化学习不依赖准确的运动学/动力学模型，也有很好的运动表现，特别是在复杂任务（多过程/难建模）中取得了不错的成绩。



#### IssacGym

> https://developer.nvidia.com/isaac-gym#:~:text=This%20release%20aligns%20the%20PhysX%20implementation%20in%20standalone%20Preview%20Isaac

​	`目前IssacGym已经被Nvida官方宣布停止维护`

​	但是3年的更新和使用(2021-2024)，使得IssacGym社区有大量的优秀开源代码，也是很多RL领域论文的训练环境，可以帮助很好的入门，熟悉强化学习环境的使用。取而代之的是IssacSim，其拥有逼真的光追以及支持软体，多智能体的仿真，但是目前不同版本中更新幅度比较大。

​	安装较为友好（在ubuntu20.04和22.04下均可正常使用）



#### Wheel-Legged-Gym

> https://github.com/clearlab-sustech/Wheel-Legged-Gym

由南科大推出的轮腿强化学习的开源仓库十分适合双轮足式机器人，是一个想当不错的学习资料。



##### Install

建议使用anaconda新配置一个python3.8的环境，按照`README`中的顺序安装没有大问题。（在安装tensorflow的时候会有环境冲突，但是不影响使用）



##### Code

> 建议一遍使用一遍查看，着重于envs下的内容

仓库架构如下：

- resources：用于放置机器人urdf和meshes(stl文件)

  - 注意urdf中和stl相关的部分书写格式和ROS有些不同，ROS中采用ROS软件包作为路径名起始`<meshfilename="package://diablo/description/meshes/base_link.stl"`

  ​		而IssacGym采用相对路径`<mesh filename="../meshes/rf0_Link.STL" />`

  - 在urdf中IssacGym中的旋转关节的type类型全部采用revolute，对于无限位的旋转joint，在limit中不添加upper和lower即可，例如：

    ```
    <joint name="left_wheel_joint" type="revolute">
        <origin rpy="0 0 0.13433" xyz="0.14 0 0.025"/>
        <parent link="left_fake_shank_link"/>
        <child link="left_wheel"/>
        <axis xyz="0 0 1"/>
        <limit effort="5" velocity="26" />
      </joint>
    ```

    正常关节采用如下格式：

    ```
      <joint name="right_fake_knee_joint" type="revolute">
        <origin rpy="0 0 -2.8729" xyz="0.14 0 0"/>
        <parent link="right_fake_thigh_link"/>
        <child link="right_fake_shank_link"/>
        <axis xyz="0 0 1"/>
        <limit effort="27" lower="-1.57" upper="1.57" velocity="26"/>
      </joint>
    ```

    （在gazebo中无限位的旋转Joint应该采用`continues`，本末的Diablo的urdf中所有urdf都为`continues`需要修改后使用）

- wheel_legged_gym

  - envs：放置机器人们的配置以及利用IssacGym接口进行强化学习的python文件（最重要的）
  - rsl_rl：采用PPO算法用并和IssacGym环境交互的包（一般使用不需要修改）
  - scripts：放置`play`和`train`
  - utilis：放置地形设置，数学工具，参数获取等功能性函数辅助`envs`



##### Use

> 主要掌握怎么正确方便使用`scripts`下的`play`和`train`

​	一般的使用流程为：修改，删减reward函数和对应的config，然后train，训练完指定轮，或者看奖励值收敛后运行play看运动表现和期望表现和实际的曲线，如果合适就放在gazebo中进行sim2sim。

​	`play`和`train`都可以传很多参数，有几个最常用的：



`train.py`：

- --task=xxxxx，后面的xxx指训练的机器人的envs，在运行时最常用的例如：

```
python train.py --task=diablo
```

其中task后的名有什么可以在`envs/__init__.py`下查看，有例如：

```
task_registry.register(
    "wheel_legged", LeggedRobot, WheelLeggedCfg(), WheelLeggedCfgPPO()
)
```

 其中wheel_legged就是一个`task`名。具体来说是利用`utils`下`task_registry.py`的task_registry函数，结合`envs`中的机器人config和训练算法config来注册一个类



此外：也会遇到在指定的轮数训练完后它的奖励函数还呈现一个向上走的趋势的情况我们这个时候就可以在上一次训练完的基础上接着训练，使用如下：

```
python train.py --task=diablo --resume --load_run=xxxxxx --checkpoint=xxxx
```

- resume表示这次训练是在上一次的基础上继续训练的
- load_run后接的是对应训练的文件夹名（在每一次训练时，和`wheel_legged_gym`同级下会有一个`log`文件夹，其中会储存每一个task的每一次训练备份，以日期的形式保存）
- checkpoint即检查点，默认每100轮会生成一个检查点



​	默认的训练轮次为3000轮，如果希望可以训练更多轮（比如晚上离开前让它训练一晚上，可以通过

`--max_iterations=xxxx`来让它训练xxxx轮。



`play.py`：

​	如果什么参数都不增加，直接运行`python play.py`它会运行上一次trian训练的结果，如果要指定某一个task，某一个检查点采用如下格式：

```
python play.py --task=xxx --load_run=xxx --checkpoint=xxxx
```

​	注意如果需要sim2sim和sim2real并且是采用libtorch库，一定需要先play对应的.pt文件，应为在play中会有

```
export_policy_as_jit(ppo_runner.alg.actor_critic, path)
```

​	它会将强化学习算法中的策略（policy）模型导出为 JIT（Just-In-Time）格式，而这个格式是libtorch所需要的。



​	==说明：可以看到该仓库下的envs中有四个文件夹，base为剩余三个共用的部分，wheel_legged的obs和action都是直接关节数值，而wheel_legged_vmc中是用使用了VMC抽象后的值作为obs和action值，因为IssacGym中不支持并联仿真，但是实际机器人又是并联，所以只能这样操作。==



##### Analyze

> 在强化学习时有十分好用的工具`tensorflow`，可以实时检测到训练的每一个episode下的各项奖励的值，总奖励曲线以及ppo内相关的曲线图，可以很好的帮助我们分析原因，并对应修改，也可以判断是否收敛。

运用方式：

去往python>=3.8的环境（不能在rlgpu下运行，因为rlgpu里的python版本为3.7）
在log的上一级目录下

```
tensorboard --logdir=logs
```

然后在浏览器中进入，即可查看

```
http ://localhost:6006
```

如果不行可能是当前环境由于pip安装了其它的包，再执行一次``pip install tensorflow``（这里也是要在python>=3.8的环境里运行）



##### conclusion

​	综上我们对这个仓库的简单使用有了一个概念，可以对已有`envs`下的task进行train和play并且看到对应的曲线图。接下来就是修改已有的`envs`，添加自己的机器人的urdf和相关配置。



#### 迁移到自己的机器人

大致流程如下：

- 准备机器人对应的URDF
- 为自己的机器人创建task(按照已有的修改即可)
- 根据实际需求增减reward函数



其中URDF部分和其它仿真器（gazebo，webots）差不多，要注意的地方在上文提及

创建task的过程根据机器人模型的差异不同，所需修改的地方也不同，在下文着重介绍



#### Code

文件大体上分为两个

- roobot.py
- roobot_config.py

前者定义了机器人如何与IssacGym这个环境交互的，后者包含所有修改的参数。

##### config

> config的部分内容较少先进行说明

config包括两部分：机器人相关的和PPO算法相关的。机器人相关的会放在`xxxxCfg`类下，而PPO相关的会放在`xxxxCfgPPO`下。一般不对后者进行修改，重点关注前者。

依次介绍其各个部分（按照认为重要程度降序排列）：

- asset：定义机器人模型内容，例如URDF和一些上下限，注意`penalize_contacts_on`，和`terminate_after_contacts_on`，这两个都是写部分link的名字，前者内的link碰到地面会收到惩罚，而后者内的link碰到地面达到某些条件（多大接触力和多少时间）后会直接提前结束当前轮的envs。
- init_state：定义机器人的初始离地位姿，以及初始线速度角速度和`default_joint_angles`，特别注意这个要和urdf内的joint对应，而且它的顺序决定了joint的顺序。
- env：设置仿真的envs数量（并行训练的机器人数，并不是越大越好，在ETH的论文中有给出经验参数4096），obs和action的数量等与环境交互的参数。
- rewards：各个奖励的比例系数（类PID中的P，最终的总奖励是各个原始奖励*各个奖励的reward系数）
- normalization：很重要，因为obs和action的值如果在正负一间可以加速模型收敛，所以一般来说会通过各种normalization来将它们缩放到正负一间。
- domain_rand：通过增加各种随机值来增加机器人的鲁棒性，例如随机地面摩擦力，随机机器人质量，质心位置，随机push机器人等。
- commands：
- control：底层控制器的选取和PD参数的设置，注意`decimation`，它表示每个仿真间隔dt内policy的更新次数，它和`sim`里面的`dt`联合决定了这个控制模型的频率。为`dt`*`decimation`
- noise：用来在obs中增加噪声
- terrain：设置地形
- sim：设置仿真时间dt和重力等与仿真器相关的参数，基本不改东
- viewer：默认观察视角，实际在仿真界面可以调。



##### robot

> 主要的内容为step函数，总体来说这个文件首先创建IssacGym环境，再从IssacGym中拿到rl算法所需要的observations和reward值，最后将rl算法得到的输出action转化为电机输出力矩给IssacGym控制机器人。

> 在阅读robot文件时注重与config中的内容对应分析，以加深对各个config的理解，方便后续修改。

robot文件主要可以分为如下几个部分：

- 创建IssacGym仿真环境
- 获得仿真环境中的observations值
- 计算reward值
- 将action转化为电机力矩并传给仿真环境



`仿真环境创建`

- `create_sim`：环境基础设置和地形创建
- `_create_envs`：创建机器人实体（同时加载实体相关的随机化`domain_rand`）
- `check_termination`检查是否结束当前`envs`

- 渲染界面仿真（可以选择不查看，即`headless`）

  

`获得observations`

- `compute_observations`：计算obs的值，（同时包括添加噪声）



`计算reward值`

- `compute_reward`：具体实现逻辑为在这个`Task`的初始化时会调用`_prepare_reward_function`，在其中会将reward_function和对应的config里的scale形成顺序对应（以scale内顺序为参考）存放在`self.reward_functions`中

```
for name, scale in self.reward_scales.items():
    if name == "termination":
        continue
    self.reward_names.append(name)
    name = "_reward_" + name
    self.reward_functions.append(getattr(self, name))
```

​		然后在`compute_reward`遍历`self.reward_functions`，把所有的奖励求加权平均值（注意单个奖励值有上下限，`self.cfg.rewards.clip_single_reward * self.dt`）

```
for i in range(len(self.reward_functions)):
    name = self.reward_names[i]
    rew = self.reward_functions[i]() * self.reward_scales[name]
    rew = torch.clip(
        rew,
        -self.cfg.rewards.clip_single_reward * self.dt,
        self.cfg.rewards.clip_single_reward * self.dt,
    )
    self.rew_buf += rew
    self.episode_sums[name] += rew
```

`新增奖励函数：	`

​		所有的奖励函数名以`_reward`开头，再加具体名字（也就是config里对应的scale名字），例如当我希望机器人输出力矩不要太大时，增加一个reward来惩罚（即scale给负）输出力矩的值

```
def _reward_torques(self):
    # Penalize torques
    return torch.sum(torch.square(self.torques), dim=1)
```

​		然后在config中增加对应的：

```
class rewards:
    class scales:
        torques = -0.0001
```

​		



`将action转化为电机力矩并传给仿真环境`

- `_compute_torques`：将action经过缩放和pid计算后返回







#### 代码细节

##### _resample_commands()和update_command_curriculum()

前者通过torch.rand将整体的命令采样放到[min, max]中间，后者通过判断速度跟踪的效果来判断是否调整速度上下限。

视角锁定：play.py里    MOVE_CAMERA = True

##### Task类的奖励

##### Smooth类的奖励

##### Safety类的奖励

##### Beauty类的奖励

#### 常用的命令：

- python wheeled_bipedal_gym/scripts/train.py --task=diablo_plus --resume --load_run=Jan04_21-44-59_ --checkpoint=500 --headless --max_iterations=10000
从上一次的运行记录开始训练，自己节选需要的内容

- python wheeled_bipedal_gym/scripts/play.py --task=diablo_plus
